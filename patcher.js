(function(view){
    'use strict';
    var $ = document.querySelector.bind(document);
    var doc = view.document;
    var selectedTabId = -1;

    // TODO: just implements the patcher stuff ...

})(
    typeof self !== "undefined" && self ||
    typeof window !== "undefined" && window ||
    this.content
);
